[user]
  email = web.elektro.net@gmail.com
  name = zaquest

[alias]
  hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
[color]
	ui = auto
[core]
	excludesfile = ~/.gitignore
[pull]
	ff = only
[init]
	defaultBranch = master
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
